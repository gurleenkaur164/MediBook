# Local dev — point the frontend at your local backend
VITE_API_URL=http://localhost:5000/api
# Socket URL is auto-derived from VITE_API_URL (strips /api). Override only if different:
# VITE_SOCKET_URL=http://localhost:5000
